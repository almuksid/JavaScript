// For Loop

for(let rep = 1; rep<=10; rep++){
    console.log(`Lifting weight repitition ${rep} 🏋️‍♂️. `)
}

const language = ["JavaScript", "Python", "C++", ["HTML", "CSS", "Bootstrap"], true];

for(let i = 0; i < language.length; i++ ){
    console.log(language[i], typeof language[i]);   // 
}